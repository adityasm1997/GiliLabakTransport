<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adityasm.gililabaktransport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".home">
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".transport"
            android:label="@string/transportasi"/>
        <activity android:name=".pembayaran"
            android:label="@string/pembayaran"/>
        <activity android:name=".data" />
        <activity android:name=".bukti_pembayaran" />
        <activity android:name=".validasi_pembayaran" />
        <activity android:name=".transport2"
            android:label="@string/transportasi"></activity>
    </application>

</manifest>